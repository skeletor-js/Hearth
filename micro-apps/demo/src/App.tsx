export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>demo</h1>
      <p>
        Edit <code>src/App.tsx</code>. This is a standalone app — its own deps, its
        own Vite server — embedded in Hearth via a sandboxed iframe.
      </p>
    </div>
  )
}
